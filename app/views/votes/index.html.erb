<h2>お題投票一覧</h2>
<table>
  <tbody>
    <tr>
      <% @votes.each do |vote| %>
      <td><%= link_to vote.title, vote_path(vote) %></td>
      <% end %>
      <%= link_to "お題一覧へ", votes_path %>
    </tr>
  </tbody>
</table>
